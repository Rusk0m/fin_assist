name: fin_assist
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  json_serializable: ^6.10.0
  json_annotation: ^4.9.0
  equatable:
  get_it:
  dio:
  intl:
  intl_utils:
  dartz: ^0.10.1
  hive: ^2.2.3
  firebase_core: ^4.1.0
  firebase_auth: ^6.0.2
  google_sign_in: ^6.2.1
  cloud_firestore: ^6.0.1
  shared_preferences:
  pdf:
  printing:
  path_provider: ^2.1.4
  file_picker:
  open_filex:
  syncfusion_flutter_charts: ^31.1.19

dev_dependencies:
  build_runner:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
  generate: true
  assets:
    - lib/assets/fonts/times_new_roman.ttf
flutter_intl:
  enabled: true
